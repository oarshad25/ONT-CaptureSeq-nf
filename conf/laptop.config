params {
    config_profile_description = 'Profile for execution on my laptop'
}

process.executor = 'local'
docker.enabled = true
docker.runOptions = '-u $(id -u):$(id -g) --platform=linux/amd64'

executor.cpus = 6
executor.memory = '16 G'
